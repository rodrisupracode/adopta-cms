version: '3.8'
services:
  web:
    build: .
    image: adopta-cms:latest
    container_name: adopta-cms
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    # Uncomment the following lines if you want to mount local code for development
    # volumes:
    #   - ./:/app
